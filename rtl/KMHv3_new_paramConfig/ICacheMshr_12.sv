// Generated by CIRCT firtool-1.135.0

// Include register initializers in init blocks unless synthesis is set
`ifndef RANDOMIZE
  `ifdef RANDOMIZE_REG_INIT
    `define RANDOMIZE
  `endif // RANDOMIZE_REG_INIT
`endif // not def RANDOMIZE
`ifndef SYNTHESIS
  `ifndef ENABLE_INITIAL_REG_
    `define ENABLE_INITIAL_REG_
  `endif // not def ENABLE_INITIAL_REG_
`endif // not def SYNTHESIS

// Standard header to adapt well known macros for register randomization.

// RANDOM may be set to an expression that produces a 32-bit random unsigned value.
`ifndef RANDOM
  `define RANDOM $random
`endif // not def RANDOM

// Users can define INIT_RANDOM as general code that gets injected into the
// initializer block for modules with registers.
`ifndef INIT_RANDOM
  `define INIT_RANDOM
`endif // not def INIT_RANDOM

// If using random initialization, you can also define RANDOMIZE_DELAY to
// customize the delay used, otherwise 0.002 is used.
`ifndef RANDOMIZE_DELAY
  `define RANDOMIZE_DELAY 0.002
`endif // not def RANDOMIZE_DELAY

// Define INIT_RANDOM_PROLOG_ for use in our modules below.
`ifndef INIT_RANDOM_PROLOG_
  `ifdef RANDOMIZE
    `ifdef VERILATOR
      `define INIT_RANDOM_PROLOG_ `INIT_RANDOM
    `else  // VERILATOR
      `define INIT_RANDOM_PROLOG_ `INIT_RANDOM #`RANDOMIZE_DELAY begin end
    `endif // VERILATOR
  `else  // RANDOMIZE
    `define INIT_RANDOM_PROLOG_
  `endif // RANDOMIZE
`endif // not def INIT_RANDOM_PROLOG_
module ICacheMshr_12(
  input         clock,
  input         reset,
  input         io_fencei,
  input         io_flush,
  input         io_wfi_wfiReq,
  output        io_wfi_wfiSafe,
  output        io_req_ready,
  input         io_req_valid,
  input  [41:0] io_req_bits_blkPAddr,
  input  [5:0]  io_req_bits_vSetIdx,
  input  [41:0] io_lookUps_0_req_bits_blkPAddr,
  input  [5:0]  io_lookUps_0_req_bits_vSetIdx,
  output        io_lookUps_0_resp_hit,
  input  [41:0] io_lookUps_1_req_bits_blkPAddr,
  input  [5:0]  io_lookUps_1_req_bits_vSetIdx,
  output        io_lookUps_1_resp_hit,
  input         io_acquire_ready,
  output        io_acquire_valid,
  output [47:0] io_acquire_bits_acquire_address,
  output [5:0]  io_acquire_bits_vSetIdx,
  input  [1:0]  io_victimWay,
  output        io_info_valid,
  output [41:0] io_info_bits_blkPAddr,
  output [5:0]  io_info_bits_vSetIdx,
  output [1:0]  io_info_bits_way,
  input         io_invalid
);

  reg         valid;
  reg         flush;
  reg         fencei;
  reg         issue;
  reg  [41:0] blkPAddr;
  reg  [5:0]  vSetIdx;
  reg  [1:0]  way;
  wire        io_req_ready_0 = ~valid & ~io_flush & ~io_fencei;
  wire        io_acquire_valid_0 =
    valid & ~issue & ~io_flush & ~io_fencei & ~io_wfi_wfiReq;
  wire        _GEN = io_fencei | io_flush;
  wire        _GEN_0 = io_req_ready_0 & io_req_valid;
  wire        _GEN_1 = io_acquire_ready & io_acquire_valid_0;
  always @(posedge clock or posedge reset) begin
    if (reset) begin
      valid <= 1'h0;
      flush <= 1'h0;
      fencei <= 1'h0;
      issue <= 1'h0;
      blkPAddr <= 42'h0;
      vSetIdx <= 6'h0;
      way <= 2'h0;
    end
    else begin
      valid <= ~io_invalid & (_GEN_0 | ~(_GEN & ~issue) & valid);
      flush <= ~_GEN_0 & (_GEN | flush);
      fencei <= ~_GEN_0 & (_GEN | fencei);
      issue <= _GEN_1 | ~_GEN_0 & issue;
      if (_GEN_0) begin
        blkPAddr <= io_req_bits_blkPAddr;
        vSetIdx <= io_req_bits_vSetIdx;
      end
      if (_GEN_1)
        way <= io_victimWay;
    end
  end // always @(posedge, posedge)
  `ifdef ENABLE_INITIAL_REG_
    `ifdef FIRRTL_BEFORE_INITIAL
      `FIRRTL_BEFORE_INITIAL
    `endif // FIRRTL_BEFORE_INITIAL
    logic [31:0] _RANDOM[0:2];
    initial begin
      `ifdef INIT_RANDOM_PROLOG_
        `INIT_RANDOM_PROLOG_
      `endif // INIT_RANDOM_PROLOG_
      `ifdef RANDOMIZE_REG_INIT
        for (logic [1:0] i = 2'h0; i < 2'h3; i += 2'h1) begin
          _RANDOM[i] = `RANDOM;
        end
        valid = _RANDOM[2'h0][0];
        flush = _RANDOM[2'h0][1];
        fencei = _RANDOM[2'h0][2];
        issue = _RANDOM[2'h0][3];
        blkPAddr = {_RANDOM[2'h0][31:20], _RANDOM[2'h1][29:0]};
        vSetIdx = {_RANDOM[2'h1][31:30], _RANDOM[2'h2][3:0]};
        way = _RANDOM[2'h2][5:4];
      `endif // RANDOMIZE_REG_INIT
      if (reset) begin
        valid = 1'h0;
        flush = 1'h0;
        fencei = 1'h0;
        issue = 1'h0;
        blkPAddr = 42'h0;
        vSetIdx = 6'h0;
        way = 2'h0;
      end
    end // initial
    `ifdef FIRRTL_AFTER_INITIAL
      `FIRRTL_AFTER_INITIAL
    `endif // FIRRTL_AFTER_INITIAL
  `endif // ENABLE_INITIAL_REG_
  assign io_wfi_wfiSafe = ~(valid & issue);
  assign io_req_ready = io_req_ready_0;
  assign io_lookUps_0_resp_hit =
    valid & ~fencei & ~flush & io_lookUps_0_req_bits_vSetIdx == vSetIdx
    & io_lookUps_0_req_bits_blkPAddr == blkPAddr;
  assign io_lookUps_1_resp_hit =
    valid & ~fencei & ~flush & io_lookUps_1_req_bits_vSetIdx == vSetIdx
    & io_lookUps_1_req_bits_blkPAddr == blkPAddr;
  assign io_acquire_valid = io_acquire_valid_0;
  assign io_acquire_bits_acquire_address = {blkPAddr, 6'h0};
  assign io_acquire_bits_vSetIdx = vSetIdx;
  assign io_info_valid = valid & ~flush & ~fencei;
  assign io_info_bits_blkPAddr = blkPAddr;
  assign io_info_bits_vSetIdx = vSetIdx;
  assign io_info_bits_way = way;
endmodule

