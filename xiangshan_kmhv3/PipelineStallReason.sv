// Generated by CIRCT firtool-1.135.0

// Include register initializers in init blocks unless synthesis is set
`ifndef RANDOMIZE
  `ifdef RANDOMIZE_REG_INIT
    `define RANDOMIZE
  `endif // RANDOMIZE_REG_INIT
`endif // not def RANDOMIZE
`ifndef SYNTHESIS
  `ifndef ENABLE_INITIAL_REG_
    `define ENABLE_INITIAL_REG_
  `endif // not def ENABLE_INITIAL_REG_
`endif // not def SYNTHESIS

// Standard header to adapt well known macros for register randomization.

// RANDOM may be set to an expression that produces a 32-bit random unsigned value.
`ifndef RANDOM
  `define RANDOM $random
`endif // not def RANDOM

// Users can define INIT_RANDOM as general code that gets injected into the
// initializer block for modules with registers.
`ifndef INIT_RANDOM
  `define INIT_RANDOM
`endif // not def INIT_RANDOM

// If using random initialization, you can also define RANDOMIZE_DELAY to
// customize the delay used, otherwise 0.002 is used.
`ifndef RANDOMIZE_DELAY
  `define RANDOMIZE_DELAY 0.002
`endif // not def RANDOMIZE_DELAY

// Define INIT_RANDOM_PROLOG_ for use in our modules below.
`ifndef INIT_RANDOM_PROLOG_
  `ifdef RANDOMIZE
    `ifdef VERILATOR
      `define INIT_RANDOM_PROLOG_ `INIT_RANDOM
    `else  // VERILATOR
      `define INIT_RANDOM_PROLOG_ `INIT_RANDOM #`RANDOMIZE_DELAY begin end
    `endif // VERILATOR
  `else  // RANDOMIZE
    `define INIT_RANDOM_PROLOG_
  `endif // RANDOMIZE
`endif // not def INIT_RANDOM_PROLOG_
module PipelineStallReason(
  input        clock,
  input        io_prePipeStall,
  input  [6:0] io_prePipeStallReason,
  input        io_prePipeBubble,
  input  [6:0] io_prePipeBubbleReason,
  input        io_redirect,
  input  [6:0] io_redirectReason,
  input        io_currentPipeStall,
  input  [6:0] io_currentPipeStallReason,
  output [6:0] io_outReason
);

  reg       redirectReg;
  reg [6:0] redirectReasonReg;
  reg       prePipeStallReg;
  reg [6:0] prePipeStallReasonReg;
  reg       currentPipeStallReg;
  reg [6:0] currentPipeStallReasonReg;
  always @(posedge clock) begin
    redirectReg <= io_redirect;
    redirectReasonReg <= io_redirectReason;
    prePipeStallReg <= io_prePipeStall;
    prePipeStallReasonReg <= io_prePipeStallReason;
    currentPipeStallReg <= io_currentPipeStall;
    currentPipeStallReasonReg <= io_currentPipeStallReason;
  end // always @(posedge)
  `ifdef ENABLE_INITIAL_REG_
    `ifdef FIRRTL_BEFORE_INITIAL
      `FIRRTL_BEFORE_INITIAL
    `endif // FIRRTL_BEFORE_INITIAL
    logic [31:0] _RANDOM[0:0];
    initial begin
      `ifdef INIT_RANDOM_PROLOG_
        `INIT_RANDOM_PROLOG_
      `endif // INIT_RANDOM_PROLOG_
      `ifdef RANDOMIZE_REG_INIT
        _RANDOM[/*Zero width*/ 1'b0] = `RANDOM;
        redirectReg = _RANDOM[/*Zero width*/ 1'b0][0];
        redirectReasonReg = _RANDOM[/*Zero width*/ 1'b0][7:1];
        prePipeStallReg = _RANDOM[/*Zero width*/ 1'b0][8];
        prePipeStallReasonReg = _RANDOM[/*Zero width*/ 1'b0][15:9];
        currentPipeStallReg = _RANDOM[/*Zero width*/ 1'b0][16];
        currentPipeStallReasonReg = _RANDOM[/*Zero width*/ 1'b0][23:17];
      `endif // RANDOMIZE_REG_INIT
    end // initial
    `ifdef FIRRTL_AFTER_INITIAL
      `FIRRTL_AFTER_INITIAL
    `endif // FIRRTL_AFTER_INITIAL
  `endif // ENABLE_INITIAL_REG_
  assign io_outReason =
    io_redirect | redirectReg
      ? (io_redirect ? io_redirectReason : redirectReasonReg)
      : prePipeStallReg
          ? prePipeStallReasonReg
          : currentPipeStallReg
              ? currentPipeStallReasonReg
              : io_prePipeBubble ? io_prePipeBubbleReason : 7'h0;
endmodule

