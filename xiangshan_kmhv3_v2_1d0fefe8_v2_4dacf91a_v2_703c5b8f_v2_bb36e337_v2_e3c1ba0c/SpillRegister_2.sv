// Generated by CIRCT firtool-1.135.0

// Include register initializers in init blocks unless synthesis is set
`ifndef RANDOMIZE
  `ifdef RANDOMIZE_REG_INIT
    `define RANDOMIZE
  `endif // RANDOMIZE_REG_INIT
`endif // not def RANDOMIZE
`ifndef SYNTHESIS
  `ifndef ENABLE_INITIAL_REG_
    `define ENABLE_INITIAL_REG_
  `endif // not def ENABLE_INITIAL_REG_
`endif // not def SYNTHESIS

// Standard header to adapt well known macros for register randomization.

// RANDOM may be set to an expression that produces a 32-bit random unsigned value.
`ifndef RANDOM
  `define RANDOM $random
`endif // not def RANDOM

// Users can define INIT_RANDOM as general code that gets injected into the
// initializer block for modules with registers.
`ifndef INIT_RANDOM
  `define INIT_RANDOM
`endif // not def INIT_RANDOM

// If using random initialization, you can also define RANDOMIZE_DELAY to
// customize the delay used, otherwise 0.002 is used.
`ifndef RANDOMIZE_DELAY
  `define RANDOMIZE_DELAY 0.002
`endif // not def RANDOMIZE_DELAY

// Define INIT_RANDOM_PROLOG_ for use in our modules below.
`ifndef INIT_RANDOM_PROLOG_
  `ifdef RANDOMIZE
    `ifdef VERILATOR
      `define INIT_RANDOM_PROLOG_ `INIT_RANDOM
    `else  // VERILATOR
      `define INIT_RANDOM_PROLOG_ `INIT_RANDOM #`RANDOMIZE_DELAY begin end
    `endif // VERILATOR
  `else  // RANDOMIZE
    `define INIT_RANDOM_PROLOG_
  `endif // RANDOMIZE
`endif // not def INIT_RANDOM_PROLOG_
module SpillRegister_2(
  input          clock,
  input          reset,
  output         io_in_ready,
  input          io_in_valid,
  input  [255:0] io_in_bits_data,
  input  [31:0]  io_in_bits_strb,
  input          io_in_bits_last,
  input          io_out_ready,
  output         io_out_valid,
  output [255:0] io_out_bits_data,
  output [31:0]  io_out_bits_strb,
  output         io_out_bits_last
);

  reg          reg1stValid;
  reg  [255:0] reg1stData_data;
  reg  [31:0]  reg1stData_strb;
  reg          reg1stData_last;
  reg          reg2ndValid;
  reg  [255:0] reg2ndData_data;
  reg  [31:0]  reg2ndData_strb;
  reg          reg2ndData_last;
  wire         _GEN = ~reg2ndValid & io_in_valid;
  wire         _GEN_0 = _GEN & ~io_out_ready;
  always @(posedge clock or posedge reset) begin
    if (reset) begin
      reg1stValid <= 1'h0;
      reg2ndValid <= 1'h0;
    end
    else begin
      reg1stValid <= _GEN | ~(io_out_ready & ~reg2ndValid) & reg1stValid;
      reg2ndValid <= _GEN_0 ? reg1stValid : ~io_out_ready & reg2ndValid;
    end
  end // always @(posedge, posedge)
  always @(posedge clock) begin
    if (_GEN) begin
      reg1stData_data <= io_in_bits_data;
      reg1stData_strb <= io_in_bits_strb;
      reg1stData_last <= io_in_bits_last;
    end
    if (_GEN_0) begin
      reg2ndData_data <= reg1stData_data;
      reg2ndData_strb <= reg1stData_strb;
      reg2ndData_last <= reg1stData_last;
    end
  end // always @(posedge)
  `ifdef ENABLE_INITIAL_REG_
    `ifdef FIRRTL_BEFORE_INITIAL
      `FIRRTL_BEFORE_INITIAL
    `endif // FIRRTL_BEFORE_INITIAL
    logic [31:0] _RANDOM[0:18];
    initial begin
      `ifdef INIT_RANDOM_PROLOG_
        `INIT_RANDOM_PROLOG_
      `endif // INIT_RANDOM_PROLOG_
      `ifdef RANDOMIZE_REG_INIT
        for (logic [4:0] i = 5'h0; i < 5'h13; i += 5'h1) begin
          _RANDOM[i] = `RANDOM;
        end
        reg1stValid = _RANDOM[5'h0][0];
        reg1stData_data =
          {_RANDOM[5'h0][31:1],
           _RANDOM[5'h1],
           _RANDOM[5'h2],
           _RANDOM[5'h3],
           _RANDOM[5'h4],
           _RANDOM[5'h5],
           _RANDOM[5'h6],
           _RANDOM[5'h7],
           _RANDOM[5'h8][0]};
        reg1stData_strb = {_RANDOM[5'h8][31:1], _RANDOM[5'h9][0]};
        reg1stData_last = _RANDOM[5'h9][1];
        reg2ndValid = _RANDOM[5'h9][2];
        reg2ndData_data =
          {_RANDOM[5'h9][31:3],
           _RANDOM[5'hA],
           _RANDOM[5'hB],
           _RANDOM[5'hC],
           _RANDOM[5'hD],
           _RANDOM[5'hE],
           _RANDOM[5'hF],
           _RANDOM[5'h10],
           _RANDOM[5'h11][2:0]};
        reg2ndData_strb = {_RANDOM[5'h11][31:3], _RANDOM[5'h12][2:0]};
        reg2ndData_last = _RANDOM[5'h12][3];
      `endif // RANDOMIZE_REG_INIT
      if (reset) begin
        reg1stValid = 1'h0;
        reg2ndValid = 1'h0;
      end
    end // initial
    `ifdef FIRRTL_AFTER_INITIAL
      `FIRRTL_AFTER_INITIAL
    `endif // FIRRTL_AFTER_INITIAL
  `endif // ENABLE_INITIAL_REG_
  assign io_in_ready = ~reg2ndValid;
  assign io_out_valid = reg1stValid | reg2ndValid;
  assign io_out_bits_data = reg2ndValid ? reg2ndData_data : reg1stData_data;
  assign io_out_bits_strb = reg2ndValid ? reg2ndData_strb : reg1stData_strb;
  assign io_out_bits_last = reg2ndValid ? reg2ndData_last : reg1stData_last;
endmodule

